generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// Model for OnBoarding
model OnBoarding {
  id                String   @id @default(uuid())
  userId            String   @db.Uuid
  userName          String
  country           String?
  state             String?
  city              String?
  address           String?
  postalCode        String?
  identityDocType   String?
  identityImagesUrl String[]
  selfieImageUrl    String?
  kycComplete       Boolean  @default(false)
  lastStep          Int
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique(userId)
}

model InvestmentPlan {
  id           String       @id @default(cuid())
  name         String       @unique
  minAmount    Float
  interestRate Float // As a percentage (e.g., 10 for 10%)
  durationDays Int? // Investment duration
  createdAt    DateTime     @default(now())
  investments  Investment[] @relation("InvestmentPlan")
}

model Investment {
  id         String         @id @default(cuid())
  userId     String         @db.Uuid
  userName   String
  planId     String
  planName   String
  plan       InvestmentPlan @relation("InvestmentPlan", fields: [planId], references: [id])
  balance    Float?         @default(0)
  status     String         @default("inactive") // "active", "inactive", "cancelled"
  approvedAt DateTime?
  createdAt  DateTime       @default(now())
}

// Enum definitions
enum TransactionType {
  deposit
  withdrawal
  interest
}

enum TransactionStatus {
  pending
  approved
  rejected
}

// Transaction model
model Transaction {
  id         String            @id @default(cuid())
  userId     String
  type       TransactionType // "deposit" or "withdrawal"
  amount     Float
  method     String
  currency   String?
  status     TransactionStatus @default(pending) // "pending", "approved", "rejected"
  createdAt  DateTime          @default(now())
  approvedAt DateTime?
}
